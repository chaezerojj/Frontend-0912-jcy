1. git 설치

>> 구글 git 검색
   'Downloads'버튼 클릭하여 설치파일 다운로드
   '64-bit Git for Windows Setup'설치

2. 환경 설정(git config)
: 전역(Global) 수준에서 Git 사용자 이름과 이메일을 설정

3. gitHub에서 리포지토리 생성
> Repository name
(영문자, 숫자로 작성 / 띄어쓰기는 -하이픈으로 대체)
> Description

!무조건 public 체크!
Create Repository 버튼 클릭하여 생성

> 작업 디렉토리를 새롭게 생성!
> 아래의 명령어를 입력!

> VSC 터미널 단축키: ctrl + shift + `
> 기본 터미널 프로필 설정: git bash로 지정

echo "# Frontend-html-css-230912" >> README.md
git init
git add README.md
git commit -m "first commit"
git branch -M main
git remote add origin https://github.com/chaezerojj/Frontend-html-css-230912.git
git push -u origin main

4. 폴더의 업데이트 내용을 깃허브에 올리기
 특정 파일 스테이징
> git add fileName.txt (파일명.확장자 / 폴더명)

모든 변경 파일 스테이징
> git add .

> git commit
> git push